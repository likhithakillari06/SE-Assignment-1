# How to run Jar file
download the NearMiss.jar file
   - Go to folder where jar file is downloaded
   - at this location on adress bar of explorer write cmd
   - it will open the command prompt
   - type this command "java -jar NearMiss.jar"
   - And now jar file will execute and it will ask for user input of x and y
